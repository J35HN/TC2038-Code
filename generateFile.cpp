#include <iostream>
#include <fstream>
#include <string.h>

int main (int argc, char *argv[])
{
    std::string outputFileName = std::string(argv[1]);
    int linesToGenereta = stoi(std::string(argv[2]));

    //std::cout << "Filename: " << outputFile << ". Lines: " << linesToGenereta << std::endl;

    return 0;
}